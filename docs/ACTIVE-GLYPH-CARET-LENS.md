# Active-glyph law - CARET design lens, gated

## Provenance

The `Isaac_caret-q4-86-research-report.pdf` archive artifact is treated here as a disputed/hoax cultural artifact, not as evidence for alien technology, antigravity, or recovered hardware. Liris local copy:

```text
C:\tmp\Isaac_caret-q4-86-research-report.pdf
sha256=48DB54D0877F1265630D49F0D41F7A48D401A55460F17303CFE6FE864309B9E1
```

Tag: `UNVERIFIED_DISPUTED_PROVENANCE`. The provenance is intentionally outside the claim gate.

## Useful extraction

The transferable design lens is active symbolic geometry: a symbol/glyph/geometry whose placement is a behavior descriptor. For Q-PRISM and Asolaria this maps to:

```text
graphify_id -> fnv1a64 Host8 node PK -> graphify60d selector position -> behavior descriptor
```

That is close to BEHCS / HyperBEHCS addressing, but it must be inverted for safety. The glyph does not execute itself. The address describes behavior; the kernel/operator gate decides whether anything materializes.

## Law row

```text
QPRISMACTIVEGLYPH|handle8=...|geometry=graphify60d|behavior=represent_address|compile=0|interpret=0|fire=0|json=0
```

Meaning:

- `behavior=represent_address`: active only as representation and routing metadata.
- `compile=0`: no hidden compiler step is implied.
- `interpret=0`: no automatic interpreter may treat the row as executable.
- `fire=0`: AgentTerms/FEDENV, provider fanout, hardware fire, and Asolaria OS materialization remain operator-gated.
- `json=0`: HBP/HBI tuple-text is the carrier.

## Q-PRISM consequence

Each absorbed neural/event cube can carry an active-glyph descriptor so recall, atlas, and graphify can map it as a first-class 60D node. It is still a cold representation surface until the on-metal kernel receives a separate explicit execution gate.

## Handle merge

The canonical graph node handle is 
The canonical graph node handle is `node_handle8 = fnv1a64(graphify_id)`. Content references stay separate as `source_handle8 = sha256_prefix_8(source)` and `tuple_handle8 = sha256_prefix_8(tuple)`. This prevents the graph primary key from becoming a parallel content hash while keeping content-address verification available.
